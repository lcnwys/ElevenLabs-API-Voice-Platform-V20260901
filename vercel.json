{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/[...path]" }
  ],
  "functions": {
    "api/[...path].ts": {
      "includeFiles": "{dist/**,server.ts}"
    }
  }
}
